<link rel="stylesheet" href="{{ asset('css/style.css') }}" />

<table id="customers">
    <tr>
        <th>id</th>
        <th>Voornaam</th>
        <th>Achter</th>
        <th>middel</th>
    </tr>
    {% for c in clients %}
        <tr onclick="document.location = 'client/edit/{{ c.id }}';">
            <td>{{ c.id }}</td>
            <td>{{ c.firstName }}</td>
            <td>{{ c.lastName }}</td>
            <td>{{ c.middleName }}</td>
        </tr>
    {% endfor %}
    </table>