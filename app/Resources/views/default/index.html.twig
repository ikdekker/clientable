<link rel="stylesheet" href="{{ asset('css/style.css') }}" />

<h1 style="color:orange; font-family:tahoma">Clientable</h1>

<button class="btn"><a href="/client/web/client/new">Nieuwe klant</a></button>

<table id="customers">
    <tr>
        <th>id</th>
        <th>Voornaam</th>
        <th>Achter</th>
        <th>middel</th>
    </tr>
    {% for c in clients %}
        <tr onclick="document.location = 'client/edit/{{ c.id }}';">
            <td>{{ c.id }}</td>
            <td>{{ c.firstName }}</td>
            <td>{{ c.lastName }}</td>
            <td>{{ c.middleName }}</td>
        </tr>
    {% endfor %}
    </table>